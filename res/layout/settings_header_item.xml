<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/settingsHeaderTextView"
        style="@style/colorBlackText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="15dp"
        android:text="@string/amountTextView"
        android:textColor="#FFFFFF"
        android:textSize="22sp"
         />

    <!-- <View
        android:layout_width="fill_parent"
        android:layout_height="1dp"
        android:layout_alignLeft="@id/settingsHeaderTextView"
        android:layout_below="@id/settingsHeaderTextView"
        android:layout_marginRight="20dp"
        android:background="#00D5ED" /> -->

</RelativeLayout>