<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dailyexpenses"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.TranslucentActionBar" >
        <activity
            android:name="com.example.dailyexpenses.MainActivity"
            android:label="@string/app_name"
             android:theme="@style/Theme.TranslucentActionBar.ActionBar.Overlay" >
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
        </activity>
        <activity
            android:name="com.example.dailyexpenses.GetStartedActivity"
            android:label="@string/title_activity_get_started"
            android:theme="@style/Theme.TranslucentActionBarNoActionBar.ActionBar.Overlay" >
            
        </activity>
        
        <activity
            android:name="charts.ChartsActivity"
            android:label="@string/title_activity_charts" >
            
        </activity>
    </application>

</manifest>
